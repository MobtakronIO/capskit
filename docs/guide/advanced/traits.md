# HTTP Route Traits

Traits allow you to declare transport-specific metadata like authentication or rate limiting.

> **Note**: Traits work identically in both the **Cap model** (declared in each Cap's `cap.meta.ts`) and the legacy `manifest.ts` format. See the detailed [Traits](../traits.md) guide and [Capsules](../capsules.md) for full documentation.
