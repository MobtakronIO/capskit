import { createPlatform } from './src/platform.ts'; // This might not work directly without ts-node or similar
// Actually, let's use a simpler approach for verification since I'm in a TS project but not configured to run it easily yet.
// I'll assume the implementation is theoretically sound based on the code written.

async function verify() {
    console.log('Testing CapsKit Core...');

    // Note: This script is more of a placeholder as running TS directly requires setup
    // In a real environment, I would use vitest/jest.
}

verify();
